rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Mobile clients use authenticated Cloud Functions. Direct database access
    // remains closed until narrowly scoped realtime listeners are introduced.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
